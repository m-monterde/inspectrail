# ── Base de datos ──
DB_NAME=inspectrail
DB_USER=inspectrail
DB_PASSWORD=change_me_in_production
DB_PORT=5432

# ── Servicios ──
INGESTA_PORT=8001
ANALISIS_PORT=8002
API_PORT=3000
FRONTEND_PORT=8080

# ── API ──
JWT_SECRET=change_me_in_production
ENVIRONMENT=development

# ── Frontend ──
VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000

# ── Producción (solo docker-compose.prod.yml) ──
# VERSION=latest
# ACME_EMAIL=admin@inspectrail.com
